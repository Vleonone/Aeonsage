---
summary: "CLI reference for `aeonsage reset` (reset local state/config)"
read_when:
  - You want to wipe local state while keeping the CLI installed
  - You want a dry-run of what would be removed
---

# `aeonsage reset`

Reset local config/state (keeps the CLI installed).

```bash
aeonsage reset
aeonsage reset --dry-run
aeonsage reset --scope config+creds+sessions --yes --non-interactive
```


