---
summary: "CLI reference for `aeonsage uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `aeonsage uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
aeonsage uninstall
aeonsage uninstall --all --yes
aeonsage uninstall --dry-run
```


